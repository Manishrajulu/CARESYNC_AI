
  # Build Rich UI

  This is a code bundle for Build Rich UI. The original project is available at https://www.figma.com/design/PoHjFoc62iif9GF7fZvKiY/Build-Rich-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  